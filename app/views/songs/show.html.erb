<%= @song.name %>のコール一覧
<div class="entry_button" style="float: right">
  <a href="/songs/<%= @song.id %>/calls/new">コールを投稿する</a>
</div>


<% if @song.calls.present? %>
  <% @song.calls.each do |call| %>
    <li>
      <p>投稿者 :
        <a href="/users/<%= call.user.id %>">
          <%= call.user.nickname %>
        </a>
      </p>
      <p>
        投稿日時 : <%= call.created_at %>
      </p>
      <% if call.created_at != call.updated_at %>
        <p>
          更新日時 : <%= call.updated_at %>
        </p>
      <% end %>
      <p>
        動画のURL : <%= call.movie_url %><br>
        コールの説明 : <%= call.text %>
      </p>
      <a href="/songs/<%= @song.id %>/calls/<%= call.id %>">コールの詳細ページへ</a>
    </li>
  <% end %>
<% else %>
  <p>まだ投稿はありません</p>
<% end %>
